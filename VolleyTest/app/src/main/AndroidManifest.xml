<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cx.sh.cn.volleytest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cx.sh.cn.volley.custom.made.Main2Activity" />
        <activity android:name=".cx.sh.cn.load.network.Main3Activity" />
        <activity android:name=".cx.sh.cn.cx.sh.cn.cache.network.Main4Activity" />
        <activity android:name=".cx.sh.cn.cx.sh.cn.cache.network2.Main5Activity"></activity>
    </application>

</manifest>